# 01 页面导航与 UI 事件

## 目标

约束所有 draw.io 页面对应的 Screen ID、按钮事件和页面跳转结果，保证 UI 与玩法服务层路由状态一致。

## 页面枚举

```csharp
public enum ScreenId
{
    VisualStyleReference,
    MainMenu,
    TrainingModeSelection,
    ZeroingBriefing,
    ZeroingHud,
    ZeroingImpactAnalysis,
    ZeroingFinalRating,
    MovingTargetSettings,
    MovingTargetHud,
    MovingTargetResults,
    TrenchMapSelection,
    TrenchBriefing,
    TrenchHud,
    SquadCommandRadial,
    TrenchResults,
    UrbanMapSelection,
    UrbanStreetHud,
    UrbanBuildingHud,
    UrbanResults
}
```

## 路由接口

```csharp
public interface IUIRouter
{
    ScreenId Current { get; }
    ServiceResult<ScreenId> Open(ScreenId screen, NavigationArgs args = default);
    ServiceResult<ScreenId> Back();
    bool IsTransitioning { get; }
}

public readonly struct NavigationArgs
{
    public TrainingMode? Mode { get; init; }
    public string SessionId { get; init; }
    public string ReturnToScreen { get; init; }
}
```

## UI 事件命名

```csharp
public enum UIEventId
{
    MainMenu_OpenZeroing,
    MainMenu_OpenMovingTarget,
    MainMenu_OpenTrench,
    MainMenu_OpenUrban,
    Mode_SelectZeroing,
    Mode_SelectMovingTarget,
    Mode_SelectTrench,
    Mode_SelectUrban,
    Mode_Confirm,
    Zeroing_Start,
    Zeroing_ApplyAdjustment,
    Zeroing_NextRound,
    Zeroing_BackToMainMenu,
    Zeroing_Retry,
    Zeroing_BackToModeSelection,
    MovingTarget_Start,
    MovingTarget_Retry,
    MovingTarget_BackToModeSelection,
    Common_Back,
    Common_Retry,
    Trench_SelectMap,
    Trench_Start,
    Trench_ViewMap,
    Trench_Retry,
    Trench_BackToMainMenu,
    Urban_SelectMap,
    Urban_EnterBuilding,
    Urban_ExitBuilding,
    Urban_OpenRoomDoor,
    Urban_MarkRoomSearched,
    Urban_Retry,
    Urban_BackToMainMenu
}
```

## 页面跳转表

| 来源页面 | UI 事件 | 服务命令 | 目标页面 |
|---|---|---|---|
| MainMenu | MainMenu_OpenZeroing | `SetMode(Zeroing)` | ZeroingBriefing |
| MainMenu | MainMenu_OpenMovingTarget | `SetMode(MovingTarget)` | MovingTargetSettings |
| MainMenu | MainMenu_OpenTrench | `SetMode(Trench)` | TrenchMapSelection |
| MainMenu | MainMenu_OpenUrban | `SetMode(Urban)` | UrbanMapSelection |
| TrainingModeSelection | Mode_Confirm | `SetMode(selected)` | 对应模式入口 |
| ZeroingBriefing | Zeroing_Start | `ZeroingService.StartSession()` | ZeroingHud |
| ZeroingImpactAnalysis | Zeroing_ApplyAdjustment | `ZeroingService.ApplyAdjustment()` | 当前页 |
| ZeroingImpactAnalysis | Zeroing_NextRound | `ZeroingService.Continue()` | ZeroingHud、ZeroingFinalRating 或 MainMenu |
| ZeroingImpactAnalysis | Zeroing_BackToMainMenu | `TrainingSessionService.End()` 或取消当前 Session | MainMenu |
| ZeroingFinalRating | Zeroing_Retry | 清空当前 100m Session | ZeroingBriefing |
| ZeroingFinalRating | Zeroing_BackToModeSelection | 保存最近评级摘要 | TrainingModeSelection 或 MainMenu |
| MovingTargetSettings | MovingTarget_Start | `MovingTargetService.StartSession()` | MovingTargetHud |
| MovingTargetResults | MovingTarget_Retry | 清空当前移动靶 Session | MovingTargetSettings |
| MovingTargetResults | MovingTarget_BackToModeSelection | 结束 Session | TrainingModeSelection 或 MainMenu |
| TrenchMapSelection | Trench_SelectMap | `TrenchService.SelectMap()` | TrenchBriefing |
| TrenchBriefing | Trench_Start | `TrenchService.StartSession()` | TrenchHud |
| TrenchHud（Later） | 队友菜单输入（P3不开放） | 无P3命令 | SquadCommandRadial（Later） |
| UrbanMapSelection | Urban_SelectMap | `UrbanService.SelectMap()` 后 `StartSession()` | UrbanStreetHud |
| UrbanStreetHud | Urban_EnterBuilding | `UrbanService.EnterBuilding()` | UrbanBuildingHud |

## 路由约束

- `IsTransitioning == true` 时，UI 必须禁用所有会导致切页的按钮。
- `Back()` 必须返回上一业务页面，而不是固定返回主菜单。
- 打开环形菜单属于叠层 UI，关闭后应回到原 HUD。
- P1/P2 进入训练场景后先显示正前方准备页面；Start 成功只进入 `AwaitingWeaponPickup`，有效拾枪后才隐藏大型 UI 并进入实弹/倒计时。
- P1 `Zeroing_NextRound` 成功后必须隐藏弹着分析；若武器已经释放，仍保持大型 UI 隐藏，仅由最小 HUD 提示重新取枪，重新有效持枪前禁止射击。
- P1/P2 完成事件必须自动打开对应分析/结算页面，不要求玩家通过场景按钮触发。

## 事件传递

```csharp
public readonly struct UICommand
{
    public UIEventId EventId { get; init; }
    public ScreenId SourceScreen { get; init; }
    public string PayloadJson { get; init; }
    public double ClientTime { get; init; }
}
```

UI 只发送事件和参数，不直接调用场景物体；Presenter 或 Controller 将事件转成服务命令。

## P3 v1 补充

P3 新端口、配置、错误/幂等、会话修订、场景ID与Fixture遵循 [14-P3战斗契约与独立测试基础](14-P3战斗契约与独立测试基础.md)。共用类型保持唯一，生产实现不引用替身。

Urban_ExitBuilding返回街道；OpenRoomDoor/MarkRoomSearched更新当前HUD。两模式Retry取消旧局后回准备，返回主菜单先保存最近摘要；Busy/失败不切页。Trench_ViewMap为简报地图叠层。
