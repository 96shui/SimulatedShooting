# 阶段二 P2 任务进度

## 统计口径

- 最近核对日期：2026-08-05。
- 核对基线：`main` / `origin/main`，提交 `22ea2286e82421c2a6a8d2d58b72b6e3ee4fcdef`。
- 核对时工作区：clean；本文件及 P2 任务规格属于本次核对后的文档改动。
- `[x]`：实现、对应自动化测试和必要验收证据均已完成。
- `[~]`：已有主要实现，但缺接口联调、测试或验收证据。
- `[ ]`：尚未开始，或尚未满足前置门禁。

## 当前阶段判定

**项目仍处于 P1 端到端联调收口，P2 仅完成任务规格编制，尚未进入实现。**

P1 当前仍是 16 项中 13 项完成、2 项部分完成、1 项未完成。未完成的 P1 task014 仍缺由无 VR 输入替身贯穿菜单、场景、武器、3 发、分析、调整和结算的完整 PlayMode 用例，以及三轮不及格端到端用例；P1 task015/task016 仍受该门禁和最终复验约束。

## 总体进度

| 指标 | 数量 |
|---|---:|
| P2 总任务数 | 18 |
| 已完成 | 0 |
| 部分完成 | 0 |
| 未开始/受门禁阻挡 | 18 |
| 完整完成率 | 0% |

## 任务列表

下表“依赖”只列开始实现前必须满足的硬前置；可以使用冻结接口假实现并行的联调对象不计入此列。每份任务规格中的“联调依赖/后续依赖”仍是标记该任务或 P2 整体完成前必须满足的真实接入门禁。

| 状态 | 编号 | 负责范围 | 任务 | 当前说明 |
|---|---|---|---|---|
| [ ] | task001 | 功能A-玩法流程与规则 | P2 契约、P1 回归入口与测试基线冻结 | 等待 P1 task014-task016 收口；规格已建立。 |
| [ ] | task002 | 功能A-玩法流程与规则 | 设置服务、Pending 状态与本地持久化 | 依赖 task001。 |
| [ ] | task003 | 功能B-武器输入与AI基础 | 武器目录、装备状态与跨模式弹药复用 | 依赖 task001、P1 task005、P1 task013。 |
| [ ] | task004 | 功能A-玩法流程与规则 | 移动靶核心状态机、路线进度与评级 | 依赖 task001。 |
| [ ] | task005 | 场景 | 移动靶靶场 Blockout 与路线绑定 | 依赖 task001。 |
| [ ] | task006 | 功能B-武器输入与AI基础 | 移动靶两发点射、命中与昼夜瞄具接入 | 依赖 task003-task005。 |
| [ ] | task007 | 功能A-玩法流程与规则 | 跨模式 HUD、移动靶结算与评级摘要聚合 | 依赖 task003、task004、task006。 |
| [ ] | task008 | UI | 移动靶入口与模式设置 UI | 依赖 task001、task004。 |
| [ ] | task009 | UI | 移动靶白天/夜晚 HUD UI | 依赖 task006、task007。 |
| [ ] | task010 | UI | 移动靶训练结算 UI | 依赖 task007。 |
| [ ] | task011 | 场景 | 昼夜环境、目标运动与微光镜表现接入 | 依赖 task005、task006。 |
| [ ] | task012 | 场景 | 武器库武器墙与展示挂点 | 依赖 task001、task003。 |
| [ ] | task013 | UI | 武器库目录、预览与装备 UI | 依赖 task003、task012。 |
| [ ] | task014 | 功能B-武器输入与AI基础 | 设置运行时适配与训练 Session 保活 | 依赖 task002、P1 task003、P1 task013。 |
| [ ] | task015 | UI | 设置 UI、实时预览与训练中返回 | 依赖 task001、task002、task014。 |
| [ ] | task016 | 功能A-玩法流程与规则 | P2 端到端 PlayMode 门禁与 P1 回归 | 依赖 task002-task015。 |
| [ ] | task017 | UI | P2 UI 参考图、测试 ID 与 VR 可读性收口 | 依赖 task008-task010、task013、task015、task016。 |
| [ ] | task018 | 场景 | P2 场景视觉、性能与总联调收口 | 依赖 task005、task011、task012、task016、task017。 |

## P2 实现入口条件

1. P1 task014 的首轮优秀和三轮不及格端到端 PlayMode 流程通过。
2. P1 task015 的 MainScene VR 菜单可读性、左右手射线点击和测试 ID 完成复验。
3. P1 task016 完成当前 HEAD 的场景、性能和总联调回归。
4. P2 task001 同步更新必要 BDD、接口文档和测试契约，消除点射、弹药、路线、Session、路由、HUD、设置和绑定歧义。

## 本次文档交付说明

- 本次仅编写和审计任务规格，没有实现 P2 代码、场景或 UI。
- 本次没有运行 Unity EditMode、PlayMode 或 VR 实机测试，因此不得将规格完成记作功能完成。
- P2 每项任务完成时仍必须提交可追溯到具体 BDD 场景的测试；缺测试只能标记为未完成或部分完成。
