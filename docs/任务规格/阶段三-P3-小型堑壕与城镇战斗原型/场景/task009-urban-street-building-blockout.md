# task009：小型城镇街道、三层建筑与房间白盒

## 任务信息

| 项目 | 内容 |
|---|---|
| 产品阶段 | P3：小型堑壕与城镇战斗原型 |
| 负责人 | 场景 |
| 初始状态 | 已分配，未开始；本文件不是实现完成证明 |
| 分配日期 | 2026-09-09 |
| 审核负责人 | 审核测试与优化，按阶段审核清单独立复验 |

## 目标

提供一条街、一栋三层建筑、总计 3–5 个可搜索房间的可通行场景和完整绑定数据。

## 前置条件与开工方式

节点 A；可复用本线 task003 的绑定工具。不依赖真实 UrbanService、房间服务或 UI。

共同规则：先读 [阶段 README](../README.md)、[契约冻结清单](../契约冻结清单.md) 和本任务引用。跨线只依赖冻结契约和测试替身；真实集成才依赖其他线的已交付产物。若实现需要改变已有行为/接口，先更新 BDD、接口和任务，再更新测试及实现。

## BDD 追溯

| BDD 文件 | 本任务负责的场景 |
|---|---|
| [18-城镇地图选择](../../../BDD/screens/18-城镇地图选择.feature.md) | 显示城镇任务信息、选择地图进入城镇任务 |
| [19-城镇街道HUD](../../../BDD/screens/19-城镇街道HUD.feature.md) | 建筑入口显示可交互提示、进入建筑搜索阶段 |
| [20-建筑搜索HUD](../../../BDD/screens/20-建筑搜索HUD.feature.md) | HUD 显示楼层搜索信息、房门可交互时显示提示 |
| [21-城镇任务结算](../../../BDD/screens/21-城镇任务结算.feature.md) | 显示三层楼平面缩略图 |

测试方法名或测试说明必须记录上表的文件和场景名。同一场景跨层时，本任务验证本层职责，最终真实闭环由 task013 覆盖。文件中的 Later 场景不因被引用而纳入本任务。

## 接口与布局依据

- [03-HUD显示数据.md](../../../接口文档/03-HUD显示数据.md)
- [09-城镇任务服务.md](../../../接口文档/09-城镇任务服务.md)
- [11-Unity场景与Prefab约定.md](../../../接口文档/11-Unity场景与Prefab约定.md)
- [UI 线框图](../../../../UI/Sample/vr-shooting-ui-reference-wireframes.drawio)：保留既有页面结构；缺失行为先补规格，不以线框示例数值作为业务常量。

## 实现内容

### 1. 数据与入口

创建/补齐 Assets/Scenes/CombatScene.unity（UrbanScene 逻辑区域）：一条街、一栋三层楼、总计 3–5 个房间（不是每层 3–5 个）；楼梯连通各层且可返回街道。

### 2. 状态与交互

提供玩家/两队友出生、武器、HUD/结算锚点和 BuildingEntranceId；FloorId、RoomId、门、搜索区与地图配置逐项对应。

### 3. 服务与适配

布置街道 1–2 名、建筑 3–6 名敌人的候选点，建筑点覆盖楼道拐角/房间；敌人数量选择和随机分布留给服务。

### 4. 边界与幂等

输出街道底图、1F/2F/3F 楼层边界和缩略图资源键，提供世界坐标映射配置以供玩法聚合地图 DTO。

### 5. 事件与生命周期

建立门口/入口范围、房间检查区和基础环境 Collider；触发器只报进入/退出/交互事实，不自行标记房间已搜索。

### 6. 交付与兼容

校验入口、楼层、房间 ID 唯一，出生和候选点不在墙内；楼梯、门洞尺寸适合三人队列及武器碰撞。

## 交付物

- UrbanScene 白盒、楼层/房间/入口配置、候选点与搜索区绑定表。
- 街道及三层平面图资源、UI/角色/武器锚点。
- 场景 EditMode/PlayMode、无 VR 巡检路径与平面截图。

交付记录另建 `task009-交付记录.md`，至少包括：实际变更路径、BDD/接口/测试映射、测试命令与结果文件、提交号、使用的替身、联调状态、已知问题及 VR 待验项。交付记录不另占任务编号。

## 测试要求

边界修复回归：外围建筑间及后方有连续地面，城镇外圈围墙阻挡直行和斜向步行；保留战壕连接口及原有楼层/房间通行。出生及复位后稳定站立。追溯 BDD 19 场景边界补充验收。

先补或更新本任务的自动化测试，再实现业务/适配逻辑。以下均为待新增或待更新的测试要求，不代表当前已有通过结果。

- EditMode：恰好三层、总计 3–5 房间、入口/门/搜索区完整、唯一 ID 与配置一致。
- PlayMode：从街道经入口上到各层再返回；出生、门洞、楼梯和全部检查区域可达。
- PlayMode：Fake 场景事实输入能标识当前入口/楼层/房间；无 Missing Script 或对真实 UI/玩法硬引用。

测试资产应放入现有适用的 EditMode/PlayMode 程序集，按功能选择 `Assets/VRShooting/Tests/` 或 `Assets/SimulatedShooting/Tests/`；无需为每个 task 再建一套测试框架。VR 相关功能必须保留无设备测试路径。

## 验收标准

- 地图规模和 ID 明确，可供房间/搜索/导航 Fixture 独立运行。
- 场景不自动创建战斗 Session，不决定敌人数、房间完成度或胜负。
- 交付物、测试和证据齐全；新增错误不得遗留，已知警告逐项说明。
- 对应审核节点通过后才可标记 `[x]`；只有代码、Fake 演示或自测报告时标记 `[~]`。
- 必须实机确认的交互/舒适度项目未测时保留“待验”，阶段收口由节点 D 单独判定。

## 联调对象与边界

- 玩法 task010：EntranceId/FloorId/RoomId、候选点、区域输入、地图坐标配置。
- UI task008/011：地图资源和锚点。
- 场景 task012：增量加门动画、NavMesh、角色表现并保留所有稳定 ID。

## 不包含

不做第二张地图、多栋可进入建筑、大型开放街区或复杂门锁系统。

2026-09-13 美术范围补充：按用户反馈增加六栋外围战损建筑，形成街区。现有三层楼仍为可搜索建筑；新增建筑使用封闭外立面和实体碰撞，不新增房间、入口交互或搜索任务，保留街道与战壕连接路线，不设置路牌式文字。

## 无 VR 与 VR 交付

- 无 VR：交付可复现的输入/DTO/场景 Fixture 操作步骤，不能要求人工改 Hierarchy 才能正常启动。
- VR：与本任务有关的移动、自然机瞄、UI 可读性、交互可达性或队友遮挡，写明设备、步骤、预期和结果；未涉及设备体验的纯规则任务注明不适用并给出联调入口。

## 本次场景交付范围澄清（2026-09-10）

按用户要求，task003/006 的堑壕与 task009/012 的城镇合并在 `Assets/Scenes/CombatScene.unity`，空间连续可通行。仅交付场景层，原文 TrenchScene/UrbanScene 保留为逻辑区域 ID，不再分别生成两个场景文件。交互事实、门/角色反馈与 Fake 巡检属于本次；真实玩法服务、业务 UI、任务衔接及胜负规则由其他线负责。场景本地端口见 [合并场景表现绑定](../../../接口文档/14-P3合并场景表现绑定.md)。不将 Fake 演示宣称为真实战斗闭环或契约节点 A 签核。


## 2026-09-14 场景通行修复

按用户要求，现有六栋外围建筑均开放首层入口和内部空间，每栋至少放置一个静态假人（复用角色视觉，不计入生产敌人数或搜索规则）。主楼保留三层五房和巡检角色。可行走地面必须连续承托；楼梯、平台和楼层外露边缘设置可见且与碰撞一致的防坠护栏；移除旧街道内部边界障碍。保持保存场景的建筑位置、外观和既有服务端口。

验收：无 VR CharacterController 从街道逐栋进入并返回，检查假人；网格采样地面，逐段向高处边缘直行和斜向挤压，不能坠落。对应 CombatScenePlayModeTests 的 Bdd19 通行与防坠测试。VR 实机舒适度待验。

## 2026-09-14 台湾街景与真实战壕美术迭代

用户明确要求“台湾街景 + 真实战壕”。在现有 CombatScene 内保留建筑、门洞、假人、通行与防坠结构，更新磁砖外墙、繁体中文虚构商铺招牌、骑楼雨棚、铁窗、冷气外机、屋顶水塔、机车停放区和道路设施；战壕增加湿泥、排水细节、支撑件、沙袋使用痕迹。替换泛化废墟天空和不匹配的部分建筑装饰。此要求取代此前“不设置路牌式文字”的美术限制；文字是环境店招，不是业务 UI。

保持场景表现层边界，不新增生产玩法或现实作战信息。新增实体设施提供贴合视觉的碰撞，重烘焙导航，回归所有房屋进出和防坠。验收包含保存场景结构测试、完整 CombatScene PlayMode 回归、近景/街道/战壕截图和 Editor 性能采样；VR 实机画质与性能仍需设备验收。

## 2026-09-15 台湾街景与战壕迭代验证

已保存至 `Assets/Scenes/CombatScene.unity`。加入六组繁体商铺招牌、骑楼雨棚与柱、12 辆机车、16 个屋顶水塔、18 个冷气外机、铁窗/玻璃、道路标线、路牌、电线及排水格栅；现有战壕 20 段地面补充湿泥和排水表现。店铺保留假人、入口和室内柜台，清理进入室内的旧土坡装饰。静态细节按区域及材质合并，并删除合并后不再使用的空叶节点。

`CombatSceneRenderProfile` 仅在运行场景期间启用专用 URP 配置（90m 阴影、4 级级联、4x MSAA），退出/禁用时恢复原设置。编辑器截图临时应用同一配置后立即恢复。项目公共画质设置保持原值。新资源及许可证记录在 `Assets/SimulatedShooting/Art/Combat/TaiwanStreet/ATTRIBUTION.md`。

Unity 2022.3.62f3c1 最终结果：

- PlayMode：15/15，通过房屋双向通行、从街道逐栋往返、防坠、房门、假人复位及渲染配置恢复。结果：`Logs/Scene3/taiwan-optimized-playmode.xml`。
- EditMode：9/9，通过场景绑定、繁体字形、环境要素、批处理与既有场景结构检查。结果：`Logs/Scene3/taiwan-optimized-editmode.xml`。
- 截图：`Logs/Scene3/Taiwan/street.png`、`shop.png`、`district.png`、`trench.png`。
- 批处理性能样本：`Logs/Scene3/performance.txt`，RTX 3060 Laptop GPU，640x480，GPU 时长不可用，不能据此宣称 VR 达到目标帧率。实机画质、舒适度与帧预算待验。

当前仍为基于现有白盒建筑的写实化游戏场景；本次未将建筑替换成实景扫描资产，不宣称照片级复刻。

### 2026-09-15 实际素材替换结果（部分完成）

已将保存场景的 12 辆机车替换为 Scopia 的 scooter2.obj（Sweet Home 3D 官方分发，CC BY 3.0）。原始 OBJ、MTL、贴图及许可证位于 TaiwanStreet/ScopiaScooter。车长按 1.9m 统一，使用三种 URP 车漆、源材质颜色和灯具贴图，保留源模型 Mesh 引用与匹配的碰撞。旧基础几何机车已移除。

巡检界面与路牌中文统一为台湾繁体，并绑定 Noto Sans TC；技术 ID 和事实事件值未变。重新生成并保存招牌字形图集。

验证：`Logs/Scene3/real-assets-editmode.xml` 10/10，`Logs/Scene3/real-assets-playmode.xml` 15/15，均通过。机车近景为 `Logs/Scene3/Taiwan/scooter.png`。未进行 VR 实机验收。

台湾房屋替换仍未完成。已核实 House in Taipei street（tony_wu_1，CC BY 4.0，17300 faces）：https://sketchfab.com/models/dd54d8c9465742fd9f1c21e0227f6e08 。官方元数据允许下载，但下载接口要求登录。需取得模型及贴图文件后继续适配；当前保留现有建筑、入口和假人。

## 2026-09-16 外围真实台北建筑替换

移除 `EnvironmentBackdrop` 下 12 个球形 `DistantEarth` 土堆，改放 14 个取自臺北市政府都市發展局 `LOD1T_2017` I3S 服务的真实台北建筑网格。选取万华、大稻埕、中山、松山、大同与南机场附近的六个低面数节点，转换为 Unity OBJ 并保留原始影像贴图；按政府资料开放授权条款第 1 版保存来源和显名。外围资产不带碰撞，不改变城镇边界、战壕连接口、六栋可进入房屋、室内假人和敌人候选点。


## 2026-09-15 扫描人物与建筑材质补充

使用 Renderpeople Eric 免费扫描人物替换六栋房屋里的独立静态假人，保留训练演员及接口。来源与使用条款随素材记录；原文件不能作为独立素材公开分发。使用 Poly Haven / Charlotte Baglioni 的 CC0 Rectangular Facade Tiles 02 替换外墙材质并接入法线和粗糙度。原建筑、入口、碰撞和繁体文字保留。新增 EditMode 结构验收，并回归 CombatScene PlayMode 通行；整栋台北房屋模型及战壕军人模型的写实替换仍待素材，不宣称已完成。

最终使用 Eric 和 Claudia 两款扫描人物交替放置。修正旧地基顶面与室内地板共面导致的移动闪烁，两者保留至少 4cm 高差。EditMode `Logs/Scene3/floor-photographic-editmode.xml` 11/11 通过；PlayMode `Logs/Scene3/floor-photographic-playmode.xml` 15/15 通过。无 VR 头显，实机待验。

## 2026-09-15 实际素材与台湾繁体字修订

按用户反馈，街边机车使用外部实际建模素材替换基础几何拼装，保存作者及许可证；房屋优先使用有许可的台湾街屋素材。导入模型按真实尺度配置材质和贴合外形的碰撞，保持已有入口、假人、防坠和导航可用。场景店招、路牌及本地巡检界面使用台湾繁体字及 Noto Sans TC 字体；内部稳定 ID、事件字符串和按键名称保持接口不变。验收检查模型源引用、尺寸、字形，以及房屋通行回归。未取得下载文件的建筑素材须明确列为未完成，不能以白盒贴图宣称已替换。

## 2026-09-15 主楼房间连通与室内敌人布点

一、二层相邻房间的整面隔墙各改为一个 2.4m 宽、2.5m 高的永久门洞，保留走廊侧原有可交互房门。6 个建筑敌人候选点全部绑定到可搜索房间，其中顶层房间设置 2 个点；街道 2 个和战壕 5 个候选点数量不变。保存场景重烘焙 NavMesh，验证同层相邻房间可直接往返。

## 2026-09-16 接入候选

本任务场景产物从 `origin/dev/scene-p3` 的 `8fd6793` 接入当前工作分支。物理资源共用 CombatScene，按模式使用独立定义、出生点和 Session；完整验证与 VR 待验项见 [本次报告](../../../codex-reports/p3-vr-playable-integration.md)。本线工程接入不代表独立审核或阶段通过。
