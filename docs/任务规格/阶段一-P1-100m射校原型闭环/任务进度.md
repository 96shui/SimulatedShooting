# 阶段一 P1 任务进度

## 统计口径

- 统计分支：`dev/functionB`
- 基线：`main` 已包含 `task001`、`task002`
- 本分支新增：`task003`
- 仅统计当前分支中已实现、已提交、具备对应测试或明确验收证据的任务。
- 远端其他分支中的任务提交在合入当前分支或 `main` 前不计为完成。

## 总体进度

| 指标 | 数量 |
|---|---:|
| P1 总任务数 | 16 |
| 已完成 | 3 |
| 未完成 | 13 |
| 完成率 | 18.75% |

## 任务列表

| 状态 | 编号 | 负责范围 | 任务 | 当前说明 |
|---|---|---|---|---|
| [x] | task001 | 功能A-玩法流程与规则 | P1 基础契约与测试程序集规划 | 已在 `main` 合入；包含基础 DTO、枚举、通用返回结构、Runtime/EditMode/PlayMode 程序集规划和测试。 |
| [x] | task002 | 功能A-玩法流程与规则 | 路由、事件总线与训练 Session 生命周期 | 已在 `main` 合入；包含事件总线、UI 路由、训练 Session 生命周期骨架和 EditMode 测试。 |
| [x] | task003 | 功能B-武器输入与 AI 基础 | 输入适配、无 VR 测试替身与 XR 预留 | 已在 `dev/functionB` 完成；包含输入接口、无 VR 输入替身、Input System 预留适配、输入命令分发和 EditMode/PlayMode 测试。 |
| [ ] | task004 | 场景 | 100m 靶场 Blockout 与场景结构 | 当前分支未验收完成；需以任务规格和场景测试/验收记录确认后再勾选。 |
| [ ] | task005 | 功能B-武器输入与 AI 基础 | 基础武器控制：持枪、开火、换弹、切肩 | 未完成；依赖 `task003`、`task004`。 |
| [ ] | task006 | 功能A-玩法流程与规则 | 100m 射校核心规则服务 | 未完成；依赖 `task001`、`task002`、`task003`。 |
| [ ] | task007 | 功能A-玩法流程与规则 | 100m HUD 与结算 DTO 聚合 | 未完成；依赖 `task006`。 |
| [ ] | task008 | UI | 主菜单 100m 入口与任务说明 UI | 未完成；依赖 `task001`、`task002`。 |
| [ ] | task009 | UI | 100m 射击 HUD | 未完成；依赖 `task007`、`task005`。 |
| [ ] | task010 | UI | 弹着分析弹窗 UI | 未完成；依赖 `task006`、`task007`。 |
| [ ] | task011 | UI | 射校最终评级 UI | 未完成；依赖 `task006`、`task007`。 |
| [ ] | task012 | 场景 | 靶标命中与弹着可视化场景支持 | 未完成；依赖 `task004`、`task005`、`task006`。 |
| [ ] | task013 | 功能B-武器输入与 AI 基础 | 射击结果、后坐力与弹着数据接入 | 未完成；依赖 `task005`、`task006`、`task012`。 |
| [ ] | task014 | 功能A-玩法流程与规则 | P1 流程 PlayMode 测试与联调门禁 | 未完成；依赖 `task002` 到 `task013`。 |
| [ ] | task015 | UI | P1 UI 参考图还原与测试 ID 收口 | 未完成；依赖 `task008` 到 `task011`、`task014`。 |
| [ ] | task016 | 场景 | P1 场景视觉、性能与联调收口 | 未完成；依赖 `task004`、`task012`、`task014`、`task015`。 |

## 已完成任务验收摘要

### task001

- 建立 `VRShooting.Runtime` 业务程序集。
- 建立 EditMode 与 PlayMode 测试程序集。
- 实现 P1 基础 DTO、枚举、错误码、随机种子和 `UITestId`。
- 已有基础契约与 DTO 默认值相关 EditMode/PlayMode 测试。

### task002

- 实现 `IGameEventBus` / `GameEventBus`。
- 实现 `IUIRouter` / `UIRouter`。
- 实现 `ITrainingSessionService` / `TrainingSessionService` 的 P1 生命周期骨架。
- 已有事件发布订阅、页面路由、Session 生命周期相关 EditMode 测试。

### task003

- 实现 `IXRTrainingInput` 输入抽象。
- 实现 `ManualXRTrainingInput` 无 VR 输入替身。
- 实现 `InputSystemXRTrainingInput`，预留键鼠/Gamepad/XR 输入接入路径。
- 实现 `XRTrainingInputCommandDispatcher`，通过路由服务和事件总线分发输入命令，不直接操作 UI View 或玩法对象。
- 验证结果：
  - EditMode：26/26 通过。
  - PlayMode：3/3 通过。
  - 审核 agent：无阻塞问题。

## 当前关键依赖

- `task005` 已具备输入抽象前置，但仍依赖 `task004` 场景结构。
- `task006` 已具备基础契约、路由/Session 和输入前置，可以开始规则服务实现。
- P1 完整闭环仍需要功能A、功能B、UI、场景四条线继续补齐 `task004` 到 `task016`。

## 建议下一步

1. 优先确认并合入 `task004` 的 100m 靶场 Blockout 与场景结构。
2. 并行推进 `task006` 100m 射校核心规则服务。
3. 在 `task004`、`task006` 稳定后推进 `task005` 基础武器控制和 `task008` 主菜单/任务说明 UI。
