# 阶段一 P1 任务进度

## 统计口径

- 最近核对日期：2026-07-12。
- 统计分支：`dev/lty`。
- 基线：`main` / `origin/main`，提交 `1e835c9a76f0193a3b6681ed846fb8172b848bf2`（Merge pull request #11 from `UI-Design`）。
- `[x]`：实现与任务要求的对应测试或明确验收证据已合入当前基线。
- `[~]`：已有主要实现，但仍缺任务规格要求的联调、测试或验收证据，不能算完整完成。
- `[ ]`：核心交付尚未形成或未达到任务门禁。
- VR 实机验收尚未完成；无 VR 自动化完成不能替代真实头显下的舒适度、HUD 可读性和双手持枪验收。

## 总体进度

| 指标 | 数量 |
|---|---:|
| P1 总任务数 | 16 |
| 已完成 | 12 |
| 部分完成 | 3 |
| 未完成 | 1 |
| 完整完成率 | 75% |

当前阶段判定：**P1 已进入端到端联调收口阶段，尚未达到完整闭环验收；不应开始把 P2/P3 作为 P1 的隐性前置工作。**

## 任务列表

| 状态 | 编号 | 负责范围 | 任务 | 当前说明 |
|---|---|---|---|---|
| [x] | task001 | 功能A-玩法流程与规则 | P1 基础契约与测试程序集规划 | 基础 DTO、枚举、错误码、Runtime/EditMode/PlayMode 程序集与契约测试已合入。 |
| [x] | task002 | 功能A-玩法流程与规则 | 路由、事件总线与训练 Session 生命周期 | 事件总线、UI 路由、Session 生命周期及 EditMode 测试已合入。 |
| [x] | task003 | 功能B-武器输入与 AI 基础 | 输入适配、无 VR 测试替身与 XR 预留 | `IXRTrainingInput`、无 VR/Input System 适配、命令分发及 EditMode/PlayMode 测试已合入。 |
| [x] | task004 | 场景 | 100m 靶场 Blockout 与场景结构 | `ZeroingRangeScene`、100m 尺度、射击位、靶标、无 VR 相机、VR Origin 和稳定测试 ID 已合入；存在场景 PlayMode 测试与交付说明。 |
| [x] | task005 | 功能B-武器输入与 AI 基础 | 基础武器控制：持枪、开火、换弹、切肩 | 第一人称训练步枪、枪口/握把/肩侧绑定、ADS、后坐力、曳光、换弹、切肩、靶面命中和服务测试已合入；真实 VR 双手舒适度仍待实机验收。 |
| [x] | task006 | 功能A-玩法流程与规则 | 100m 射校核心规则服务 | 固定偏移、3 发/3 轮、分析、调整幂等、评级与错误分支已实现，并有 `ZeroingRulesTests` / `ZeroingServiceTests`。 |
| [x] | task007 | 功能A-玩法流程与规则 | 100m HUD 与结算 DTO 聚合 | `ZeroingHudService`、HUD/分析/结算查询和事件刷新已实现，并有 EditMode 测试。 |
| [x] | task008 | UI | 主菜单 100m 入口与任务说明 UI | 主菜单、任务说明、按钮、文本、路由和重复点击保护已实现；对应 PlayMode 测试已合入。 |
| [x] | task009 | UI | 100m 射击 HUD | 轮次、距离、弹药、稳定度、弹着、肩侧和提示均由服务 DTO/事件渲染；对应 PlayMode 测试已合入。 |
| [x] | task010 | UI | 弹着分析弹窗 UI | 弹着点、偏差、调整建议、应用调整、下一轮/查看评级流程与 PlayMode 测试已合入。 |
| [x] | task011 | UI | 射校最终评级 UI | 评级、轮次记录、重训、返回模式入口与 PlayMode 测试已合入。 |
| [x] | task012 | 场景 | 靶标命中与弹着可视化场景支持 | `TargetImpactSurface`、厘米坐标、十环判定、越界拒绝、弹着标记和测试已合入；历史结果 `unity-task012-results.xml` 为 7/7 通过。 |
| [~] | task013 | 功能B-武器输入与 AI 基础 | 射击结果、后坐力与弹着数据接入 | `WeaponShotResultEvent` 已接到 `ZeroingService`，场景开火可减少弹药并记录射校弹着，HUD 也能刷新；但尚缺“稳定度影响散布/偏移”的规则测试，以及由 `ManualXRTrainingInput` 驱动 3 发完整链路、切肩射击结果等 task013 专属 PlayMode 门禁。 |
| [ ] | task014 | 功能A-玩法流程与规则 | P1 流程 PlayMode 测试与联调门禁 | 现有测试分别覆盖菜单到 HUD、武器到射校、HUD/分析/评级页面，但没有一个由输入替身贯穿“菜单 → 场景 → 3 发 → 分析 → 应用调整 → 下一轮/结算”的完整可控结果测试，也缺 3 轮不及格端到端用例。 |
| [~] | task015 | UI | P1 UI 参考图还原与测试 ID 收口 | UI 素材、结构重构、页面测试 ID 和分页面 PlayMode 测试已合入；但受 task014 未完成影响，Busy/完整 DTO 链路尚未通过统一门禁，VR 视角可读性也未实机验收。 |
| [~] | task016 | 场景 | P1 场景视觉、性能与联调收口 | 场景侧视觉、性能预算和关键锚点已完成，历史结果分别为 10/10、11/11 通过；任务验收说明也明确标注总联调仍依赖 task014、task015 与 VR 实机验收。 |

## 当前实现与测试证据

### 已合入的主要闭环切片

- `ApplicationServices` 已组合事件总线、路由、Session、输入、武器、射校和 HUD 服务。
- `ZeroingRangeSessionBootstrap` 已在场景中复用同一套应用服务，并建立武器 Session。
- 第一人称武器开火通过 `WeaponShotResultEvent` 进入 `ZeroingService`，场景测试已验证开火后本轮剩余弹数从 3 变为 2。
- UI 已覆盖主菜单、任务说明、HUD、弹着分析和最终评级，并通过稳定 `UITestId` 定位关键控件。
- 当前仓库包含 68 个 VRShooting EditMode 测试、21 个 VRShooting PlayMode 测试和 17 个 SimulatedShooting 场景 PlayMode 测试，共 106 个测试方法。

### 已提交的历史测试结果

- `unity-task012-results.xml`：2026-07-05，7/7 通过。
- `unity-task016-results.xml`：2026-07-05，10/10 通过。
- `unity-task016-crate-results.xml`：2026-07-05，11/11 通过。

这些历史结果只证明对应提交时的场景侧用例，不替代 `1e835c9` 最新基线的全量复跑，也不覆盖 task014 的端到端门禁。

## 本次最新基线验证记录

- 从 `origin/main` 获取到最新提交 `1e835c9` 后，本地 `main` 已快进到同一提交，并从该提交创建 `dev/lty`。
- 首次通过 Unity MCP 在大量外部资源变化后运行测试时，Unity 2022.3.62f3c1 在 MCP 插件调用 `AssetDatabase.Refresh` 期间发生原生崩溃；Editor 日志保留了对应堆栈。
- 清理崩溃遗留的 `Temp/UnityLockfile` 后尝试 Unity 批处理测试，但在当前受限执行环境中 10 分钟内未生成测试日志或 XML，因此本次没有获得可作为通过依据的新鲜 EditMode/PlayMode 结果。
- 上述情况记为**当前验证环境阻塞**，不是 NUnit 用例失败；在 Unity Editor 正常重启并完成资源导入后仍需重新执行全量 EditMode 与 PlayMode 测试。

## 当前关键缺口

1. 完成 task013：让稳定度实际影响射击/弹着输入，并补齐无 VR 输入替身驱动的 3 发、HUD、肩侧与命中记录联调测试。
2. 完成 task014：建立一个真正跨 UI、场景、输入、武器、射校、HUD、分析与结算的无 VR PlayMode 夹具，至少覆盖首轮优秀和三轮不及格。
3. 在 task014 通过后复核 task015 的 Busy 状态、全部测试 ID、DTO 刷新和参考图还原。
4. 重新运行最新基线全量 EditMode/PlayMode；随后完成 task016 的总联调复跑。
5. 有真实设备后补充双手持枪、ADS 舒适度、HUD 可读性和目标设备性能验收。

## 建议下一步

1. 优先实现并测试 task013 缺口。
2. 紧接着完成 task014 端到端 PlayMode 门禁，这是 P1 从“功能切片齐全”进入“完整闭环完成”的关键一步。
3. task014 通过后对 task015、task016 做最终复验，并更新本文件为 P1 完成状态。
